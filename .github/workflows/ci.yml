name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev]"
      - run: mkdir -p data
      - run: rm -f data/test.db
      - run: DATABASE_URL=sqlite:///./data/test.db pytest
